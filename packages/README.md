# Shared Packages

This directory is reserved for shared Dart packages (such as common domain models, API clients, or design system components) when shared responsibilities emerge across multiple applications (e.g., mobile and future web admin).

Per architectural guidelines, no unnecessary packages are created prematurely.
